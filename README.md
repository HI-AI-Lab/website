# HIAI Lab Website

Static website for the Human-Inspired Artificial Intelligence Lab.

## Run locally

Open `index.html` in a browser.

## Run with Docker

```bash
docker build -t hiai-lab .
docker run -p 8080:80 hiai-lab